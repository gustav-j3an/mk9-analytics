import React from 'react'; import { Card } from './card';
interface StatCardProps{title:string;value:string|number;description?:string;icon?:React.ReactNode;iconBgColor?:string;iconColor?:string}
export function StatCard({title,value,description,icon}:StatCardProps){return <Card className="mk-kpi group relative overflow-hidden"><div className="flex items-start justify-between gap-3"><div><p className="mk-label">{title}</p><p className="mt-2 text-2xl font-bold tracking-[-.035em] text-[var(--mk-text)] tabular-nums">{value}</p></div>{icon&&<div className="mk-kpi-icon">{icon}</div>}</div>{description&&<p className="mt-3 border-t border-[var(--mk-border)] pt-2.5 text-[11px] leading-4 text-[var(--mk-text-subtle)]">{description}</p>}</Card>}
export default StatCard;