'use client';
import Link from'next/link';import{Search,X}from'lucide-react';import{useEffect,useState}from'react';import{canSearch,type SearchResult}from'@/lib/global-search';
export function GlobalSearch(){const[query,setQuery]=useState('');const[results,setResults]=useState<SearchResult[]>([]);const[loading,setLoading]=useState(false);const searchable=canSearch(query);useEffect(()=>{if(!canSearch(query))return;const controller=new AbortController();const timer=setTimeout(async()=>{setLoading(true);try{const response=await fetch(`/api/search?q=${encodeURIComponent(query)}`,{signal:controller.signal});const data=await response.json();setResults(data.results??[])}finally{setLoading(false)}},250);return()=>{clearTimeout(timer);controller.abort()}},[query]);return <div className="global-search"><Search aria-hidden="true"/><input value={query} onChange={e=>setQuery(e.target.value)} type="search" aria-label="Pesquisa global" placeholder="Buscar operação, promotor, loja..."/>{query&&<button aria-label="Limpar pesquisa" onClick={()=>setQuery('')}><X/></button>}{searchable&&<div className="search-results" role="listbox">{loading?<div className="search-message">Pesquisando...</div>:results.length?results.map(result=><Link key={`${result.type}-${result.id}`} href={result.href} onClick={()=>setQuery('')}><strong>{result.title}</strong><span>{result.subtitle}</span></Link>):<div className="search-message">Nenhum resultado encontrado.</div>}</div>}</div>}