'use client';import{useState}from'react';import{useRouter}from'next/navigation';
type Store={id:string;name:string;state:string|null;city:string|null};type Props={evidenceId:string;stores:Store[];suggestedVisitId?:string;enabled:boolean;diagnostics:unknown;suggestion:unknown;source:{file:string;sheet:string|null;row:number|null;rawStore:string;rawIndustry:string;city:string|null;state:string|null}};
export function ReconciliationActions(p:Props){const router=useRouter();const[storeId,setStoreId]=useState('');const[drawer,setDrawer]=useState(false);const[loading,setLoading]=useState('');const[error,setError]=useState('');async function act(action:string,extra:Record<string,string>={}){setLoading(action);setError('');const response=await fetch(`/api/reconciliation/${p.evidenceId}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action,...extra})});const result=await response.json();setLoading('');if(!response.ok){setError(result.error||'Falha na ação.');return}router.refresh()}return <><div className="flex min-w-[360px] flex-col gap-2"><select value={storeId} onChange={e=>setStoreId(e.target.value)} className="h-9 rounded-xl border border-[var(--mk-border-strong)] bg-[var(--mk-bg-secondary)] px-2 text-xs"><option value="">Selecionar loja canônica</option>{p.stores.map(x=><option key={x.id} value={x.id}>{x.name} · {x.city||'-'}/{x.state||'-'}</option>)}</select><div className="flex flex-wrap gap-1.5">{p.suggestedVisitId&&<Button disabled={!p.enabled} onClick={()=>act('LINK',{visitId:p.suggestedVisitId||''})}>Aceitar sugestão</Button>}<Button disabled={!p.enabled||!storeId} onClick={()=>act('CHANGE_STORE',{storeId})}>Trocar loja</Button><Button disabled={!p.enabled||!storeId} onClick={()=>act('SAVE_ALIAS',{storeId})}>Criar alias</Button><Button disabled={!p.enabled} onClick={()=>act('UNPLANNED')}>Ignorar</Button><Button disabled={!p.enabled} onClick={()=>act('REPROCESS')}>{loading==='REPROCESS'?'Processando...':'Reprocessar'}</Button><Button onClick={()=>setDrawer(true)}>Diagnóstico</Button></div>{!p.enabled&&<small className="text-[var(--mk-warning)]">Ações bloqueadas sem autenticação administrativa.</small>}{error&&<small className="text-[var(--mk-danger)]">{error}</small>}</div>{drawer&&<div className="fixed inset-0 z-50 bg-[#25231F]/70 backdrop-blur-sm" onClick={()=>setDrawer(false)}><aside className="ml-auto h-full w-full max-w-xl overflow-y-auto border-l border-[var(--mk-border-strong)] bg-[var(--mk-card-raised)] p-6 shadow-[var(--mk-shadow)]" onClick={e=>e.stopPropagation()}><div className="mb-5 flex items-center justify-between"><div><p className="text-[10px] uppercase text-[var(--mk-text-subtle)]">Diagnóstico completo</p><h2 className="text-lg font-semibold">{p.source.rawStore}</h2></div><button onClick={()=>setDrawer(false)} className="mk-secondary-button min-h-8 px-3">Fechar</button></div><dl className="grid gap-3 text-sm sm:grid-cols-2"><Info label="Arquivo" value={p.source.file}/><Info label="Aba / linha" value={`${p.source.sheet||'-'} / ${p.source.row||'-'}`}/><Info label="Loja original" value={p.source.rawStore}/><Info label="Indústria original" value={p.source.rawIndustry}/><Info label="Cidade / UF" value={`${p.source.city||'-'} / ${p.source.state||'-'}`}/></dl><Json title="Sugestão" value={p.suggestion}/><Json title="Candidatos e rejeições" value={p.diagnostics}/></aside></div>}</>}
function Button({children,...props}:React.ButtonHTMLAttributes<HTMLButtonElement>){return <button {...props} className="rounded border px-2.5 py-1.5 text-[11px] font-medium hover:bg-[#f5f5f2] disabled:cursor-not-allowed disabled:opacity-40">{children}</button>}function Info({label,value}:{label:string;value:string}){return <div><dt className="text-xs text-[var(--mk-text-subtle)]">{label}</dt><dd className="mt-1 break-words">{value}</dd></div>}function Json({title,value}:{title:string;value:unknown}){return <section className="mt-6"><h3 className="mb-2 text-sm font-semibold">{title}</h3><pre className="overflow-x-auto whitespace-pre-wrap rounded bg-[#f5f5f2] p-3 text-xs">{value?JSON.stringify(value,null,2):'Sem dados.'}</pre></section>}