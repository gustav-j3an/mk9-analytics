import{notFound}from'next/navigation';import{PageHeader}from'@/components/ui/PageHeader';import{PromoterForm}from'@/modules/promoters/components/PromoterForm';import{promoterService}from'@/modules/promoters/services/PromoterService';export const dynamic='force-dynamic';async function load(id:string){try{return await Promise.all([promoterService.getPromoterById(id),promoterService.getOptions()])}catch{return null}}export default async function EditPromoterPage({params}:{params:Promise<{id:string}>}){const result=await load((await params).id);if(!result)notFound();const[item,options]=result;return <main className="mx-auto w-full max-w-4xl space-y-6 px-4 py-8"><PageHeader category="Promotores" title={`Editar ${item.name}`} subtitle="Atualize os dados e vínculos do promotor."/><PromoterForm initial={item}{...options}/></main>}