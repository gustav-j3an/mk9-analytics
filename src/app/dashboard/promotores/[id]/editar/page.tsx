import{notFound}from'next/navigation';import{PageHeader}from'@/components/ui/PageHeader';import{PromoterForm}from'@/modules/promoters/components/PromoterForm';import{promoterService}from'@/modules/promoters/services/PromoterService';export const dynamic='force-dynamic';export default async function EditPromoterPage({params}:{params:Promise<{id:string}>}){try{const[item,options]=await Promise.all([promoterService.getPromoterById((await params).id),promoterService.getOptions()]);return <main className="mx-auto w-full max-w-4xl space-y-6 px-4 py-8"><PageHeader category="Promotores" title={`Editar ${item.name}`} subtitle="Atualize os dados e vínculos do promotor."/><PromoterForm initial={item}{...options}/></main>}catch{notFound()}}