import { notFound } from 'next/navigation';import { PageHeader } from '@/components/ui/PageHeader';import { IndustryForm } from '@/modules/industries/components/IndustryForm';import { industryService } from '@/modules/industries/services/IndustryService';export const dynamic='force-dynamic';export default async function EditIndustryPage({params}:{params:Promise<{id:string}>}){try{const item=await industryService.getIndustryById((await params).id);return <main className="mx-auto w-full max-w-4xl space-y-6 px-4 py-8"><PageHeader category="Indústrias" title={`Editar ${item.name}`} subtitle="Atualize os dados da indústria."/><IndustryForm initial={item}/></main>}catch{notFound()}}
